# docker-elk
ELK 7.6.0
```
apt install python3
python3 --version
apt install python3-pip
pip3 install -U pip
pip3 install setuptools-rust
pip3 install docker-compose
docker-compose -v
docker-compose up
